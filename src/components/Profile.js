import React from "react";

const Profile = () => {
  return <h1>Posts and Messages</h1>;
};

export default Profile;

// I feel like this is alot simpler than what I'm trying, but I can't get it to function properly but I set up the route.
